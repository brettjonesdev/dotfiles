# dotfiles
Local development scripts, aliases, shortcuts, settings

## Getting Started
Clone this repository into your home directory: `~/.dotfiles`

Then, empty out your `~/.bash_profile` and replace its contents with a single `source ~/.dotfiles/bin/index.sh`
